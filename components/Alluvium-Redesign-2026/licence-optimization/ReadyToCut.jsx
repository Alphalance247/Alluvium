import React from "react";
import OrangeBackground from "../common/orangeBackground";

const ReadyToCut = () => {
  return (
    <OrangeBackground
      title="Ready to cut waste across both platforms?"
      description="Request a licence audit. We'll show you where spend is being wasted and how to fix it, in priority order."
      btnText="Request a licence audit"
    />
  );
};

export default ReadyToCut;
